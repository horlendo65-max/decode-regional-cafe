import type {ExpenseAnalysis,WorkflowInput} from "../types.js";

// LAB TODO: Ask your coding agent to assemble the four existing components:
// ParseMpesaSkill → CategorizeSkill → BudgetMathSkill → ExpenseAdvisorAgent.
export class ExpenseAnalysisWorkflow {
 async run(_input:WorkflowInput):Promise<ExpenseAnalysis>{
  throw new Error("Complete the workflow using agent-prompts.md Step 3");
 }
}
