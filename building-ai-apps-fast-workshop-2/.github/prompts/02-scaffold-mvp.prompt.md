---
description: Scaffold the selected MVP in the starter applications.
agent: MVP Builder
---

Implement the MVP described here: `${input:spec:Paste the MVP specification}`.

Use the existing `starter/web` and `starter/api` projects. Apply the repository skills. Produce a working vertical slice, preserve the reference solution, add sample input, and run both builds.
