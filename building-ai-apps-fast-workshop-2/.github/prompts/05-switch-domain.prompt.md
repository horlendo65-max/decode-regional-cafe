---
description: Adapt the Expense Advisor architecture to another workshop domain.
agent: Domain Agent Designer
---

Adapt the existing architecture to `${input:domain:Health, Agriculture, FinTech, or another domain}` for this use case: `${input:usecase:Describe the decision tool}`.

Identify which existing components can remain unchanged, then specify only the new types, skills, agent, workflow, endpoint, and UI labels required. Hand off an exact implementation plan to the MVP Builder.
